name: requirement
stage: requirement
role: joseph
kind: worker
issueTypes:
- feature
interactive: false
staleRetryLimit: 3
routes:
  passed:
    default: design
  failed:
    default: same
  needs-info:
    default: wait
  blocked:
    default: wait-blocked
execution:
  backend: opencode
  openCodeAgent: build
openCodeAgent: build
charterPath: charter.md
promptPath: prompt.md
permissionsPath: permissions.yaml
